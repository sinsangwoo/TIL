# 2026-09-05

- 지침을 코드로 옮길 때 문자 그대로 강제하면 원래 의도를 넘어선다. "짧게 쓰라"는 문체 지침이었는데 알림 도구가 80자에서 문장을 잘라내고 있었다. 강제할 것은 결과값이 아니라 판단 기준이다.
- 도구가 안 붙어 있을 때 원인은 연결 단절만이 아니다. 이번엔 서버가 그 프로젝트 세션에 아예 로드되지 않은 것이었다. 프로세스를 직접 띄워보면 두 경우가 바로 갈린다.
- 설정 파일을 고쳐 해결한 것은 그 세션에서 검증되지 않는다. 세션 시작에 한 번만 읽는 값은 재시작 전까지 이전 상태 그대로다.
- 브라우저 자동화의 영속 프로필은 세션이 살아 있어도 로그인 자체가 만료될 수 있다. 프로필 존재와 인증 유효는 별개다.
