# 2026-08-18

- [Tools] GitHub MCP HTTP 400: 플러그인 헤더가 `Bearer ${GITHUB_PERSONAL_ACCESS_TOKEN}`인데 환경변수 미설정 → 리터럴 문자열 그대로 전송. 토큰을 `~/.claude/settings.json` env에 채우니 해결.
- [Tools] PowerShell `Get-Date -Format 'yyyy/MM'`의 `/`는 날짜 구분자로 치환됨(`2026-08`). 리터럴 슬래시는 `\/`로 이스케이프.
- [Tools] SecondBrain Daily ↔ TIL 공개 저장소 연동 구축: Stop 훅 → `til-push.ps1` (화이트리스트 add → 시크릿 가드 → 자동 커밋·푸시). 크론 없이 세션 이벤트로만 동작.
- [CS] PowerShell→curl.exe에 JSON을 `-d`로 넘기면 따옴표가 손실되어 400("malformed payload") — Git Bash curl 또는 파일 기반 전송이 안전.
